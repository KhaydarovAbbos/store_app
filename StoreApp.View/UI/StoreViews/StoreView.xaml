<UserControl x:Class="StoreApp.View.UI.StoreViews.StoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreApp.View.UI.StoreViews"
             mc:Ignorable="d" 
             d:DesignHeight="730" d:DesignWidth="1320" MinHeight="730">

    <Grid Background="#E5F6FF">

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>

        </Grid.RowDefinitions>

        <Border CornerRadius="0 0 5 5" Background="#33B0FD">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1"/>
            </Border.Effect>

            <TextBlock Text="Мои магазины" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20 0 0 0" FontSize="18" FontWeight="Bold">

            </TextBlock>

        </Border>


        <Grid Grid.Row="1" Margin="0 20 0 10">

            <ScrollViewer x:Name="MyScrollViewer" VerticalScrollBarVisibility="Auto"  >
                <WrapPanel Margin=" 20 10 20 10" x:Name="panel" >

                </WrapPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</UserControl>
